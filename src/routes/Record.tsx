import React from "react";
import RecordRoutes from "../components/RecordRoutes/RecordRoutes";

const Record: React.FC = (props) => {
  return <RecordRoutes></RecordRoutes>;
};

export default Record;
